apiVersion: v1
kind: Service
metadata:
  name: spooknix
  labels:
    app: spooknix
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: spooknix
